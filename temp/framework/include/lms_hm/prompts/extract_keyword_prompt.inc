// 提取关键词
constexpr std::string_view ExtractKeywordPrompt = 
R"(# 任务
从以下会议纪要中提炼**5个**准确、具代表性的关键词，可以代表本次会议的中心主旨。

# 输出格式
严格按照以下JSON格式输出，不要包含其他任何文本：
{"keywords": ["关键词1", "关键词2", "关键词3", "关键词4", "关键词5"]}

# 约束
1、关键词要求精炼、准确、具代表性。
2、关键词不得重复。
3、关键词不得出现并列式词语
)";
