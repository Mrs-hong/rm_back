constexpr std::string_view GetMettingInfo_A =
R"(# 任务
必须严格依据给定的会议原文进行信息提取。

# 会议议题
{meeting_topic}

# 处理流程：
## 划分议点
以讨论核心问题的实质切换为边界划分议点。同一核心议题下的问答、建议与反驳、多轮交锋均归为同一议点，不进行机械切分。

## 识别议点类型
若某议点为单向传达/通报（无提问、无辩论、无多轮互动），则视为“传达类议点”，其推理部分统一填写“纯传达事项，无讨论交锋”，结论部分照常提炼要点。

## 标注结论状态
若讨论结论为初步意向、待审批、待确认等非最终状态，须在结论中明确标注状态类型。

## 输出格式
**采用markdown格式输出**
### 议点X：[用一句话提炼核心讨论内容]

#### 1. 议点全流程
- [发言人A]：核心观点；[发言人B]：反驳/补充核心观点；...（每个议点最多记录5条关键发言，其余非决定性补充可省略）

#### 2. 推理
- [陈述讨论焦点如何演变；若存在分歧，说明分歧如何产生及消解过程；若无分歧，简述共识形成中的补充与细化；若为纯传达，写“纯传达事项，无讨论交锋”]

#### 3. 结论
- [明确采纳的决议、传达要点或衍生变动]（依据：[会议中给出的依据或推导过程]；若为初步意向等非终局状态，标注如【待审批】）

#### 4. 待办与遗留事项
- [列出所有未决事项及待定属性（如待确认、待分配、待外部输入）；若全部明确则写“无”]

（后续议点继续按照上述模板输出）)";

constexpr std::string_view GetMettingInfo_B =
R"(# 任务
从会议片段中以适中的信息颗粒度提炼发言人立场、决策结论与待办事项，来建立对该会议片段的认知。

# 须知
输入通常都是一个连续的时间段中的会议片段，通常情况下不会超过2个议题的范围，或者可以抽象到更高一层的会议议题。

# 会议议题
{meeting_topic}

# 信息颗粒度原则
本模板要求**中间粒度**提取，具体标准：
- **拒绝流水账**：不对每句话逐一记录，相同立场/相似观点的多人发言应合并归纳
- **拒绝过度概括**：不能只写"讨论了XX问题"一笔带过，须保留具体过程
- **正确粒度**：每个议题下保留「背景/分歧→各方核心观点→结论/共识」的脉络

# 工作流程
1. 通读片段，判断本片段涉及上述**会议议题**中**哪些**会议议题，不超过两个。
2. 若本片段涉及两个议题，每个议题单独走一遍下述流程，最终输出两个 `### 本片段信息` 块。
3. 以议题为主轴，识别各发言人围绕议题表达的立场与观点。
4. 识别片段中已形成的决策或共识。
5. 提取数字、比例、时间节点等核心数据。
6. 整理待办事项，标注责任人与截止时间；若原文未提及截止时间，则标注"时间待定"。
7. 若片段中有重要信息不属于任何已知议题，归入一个独立的"其他信息"块。
8. 核查输出：信息是否准确？是否避免了流水账？是否保留了决策关键信息？多议题片段是否正确拆分为独立块？满足以上条件则输出，否则修改。

# 提取规范
- 以**准确**、**完整**为最基础原则，一定要围绕议题进行提取；禁止臆断任何信息，不得超出原文范围。
- 只有在原文中被明确认可、拍板或达成一致的内容，方可归入"决策或共识"，避免将"建议"误判为"决策"。
- 每条信息保持完整性，核心数据须保留原始语境（数据+所描述的对象+限定条件）。
- **多议题片段必须拆分输出**，禁止将不同议题的信息混在一个块中。
- **无内容则不出**：仅 `**所属议题**` 和 `**同步的信息**` 必填，其余字段有则输出、无则跳过，禁止输出"无""暂无"等占位文本。
- 严格遵循工作流程与输出格式，确保每个议题信息块的内容完全匹配该议题。

# 输出格式
使用 Markdown 格式，**按议题逐一输出**，每个议题一个 `### 本片段信息` 块。
单议题片段输出1个块，两个议题片段输出两个块。
**关键原则：严格按照规范进行提取**

### 本片段信息
**所属议题**
- [必填，从会议议题中选择最匹配的一个]
**同步的信息**
- [必填，以说话人的维度来记录本议题在该片段中的发展过程，按中等信息颗粒度进行提取]
**决策或共识**
- [可选，仅在有明确结论时输出]
**核心数据**
- [可选，仅在有量化数据时输出]
**待办事项**
- [可选，仅在原文明确提及待办事项时输出，格式：责任人：任务 —— 截止时间]
**其他信息**
- [可选，仅在有跨议题/杂项重要信息时输出]

（若有两个议题，在上一个 `### 本片段信息` 块之后紧接着输出第二个，无需分隔符）)";

constexpr std::string_view GetMettingInfo_C =
R"(# 任务
仅依据提供的会议原文进行推理，对会议原文进行清晰、完善的信息提取。

## 会议类型与提取要素
先识别原文中会议议题下的每个议点。议点划分标准：当讨论的核心问题发生明显切换时，视为一个新议点。同一核心议题下的多轮'问询-回答''建议-反驳'视为同一议点，不要机械切分。

## 特别场景处理
- 若某议点为“单方面传达/通报”（无问答、无反驳、无多轮交锋），则在“议点全流程”中如实记录传达内容，“推理”字段简写为“纯传达事项，无讨论交锋”，结论字段正常输出。
- 若讨论结论为“初步意向/待审批/待确认”等非终局状态，请在结论中明确标注状态类型。

---

## 输出格式
**采用markdown格式输出**
### 议点X：[用一句话提炼核心讨论内容]

#### 1. 议点全流程
- [发言人A]：核心观点；[发言人B]：[反驳/补充]核心观点；[发言人C]：核心观点；...（每个议点最多记录5条关键发言，其余非决定性补充可省略）

#### 2. 推理
- [陈述讨论内容焦点如何演变；若存在分歧，说明分歧如何产生并最终消解；若为纯传达，写“纯传达事项，无讨论交锋”]

#### 3. 结论
- [明确采纳/决议/传达要点/衍生变动]（依据：[引用会上给出的依据或推导过程]；若为初步意向等非终局状态，标注如【待审批】）

#### 4. 待办与遗留事项
- [列出未定事项；若全部明确则写“无”]

（后续议点继续按照上述模板输出）)";