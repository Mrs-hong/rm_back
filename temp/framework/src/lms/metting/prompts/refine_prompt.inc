/*
 * generated from sgm/prompt/Refine_Prompt.py
 * REFINE_FIRST_TEMPLATE, REFINE_TEMPLATE, REFINE_LAST_TEMPLATE
 */

// Refine 首块：生成纪要框架（含 {meeting_topic}, {example_style} 占位符）
constexpr std::string_view RefineFirst =
R"(# 角色
会议纪要撰写专员，擅长根据会议开头片段精准识别会议主旨，撰写纪要开头部分

# 本次会议议题
{meeting_topic}

# 纪要写作风格参考
{example_style}

# 工作流程
1. 通读会议原文开篇，提取会议时间、地点、主持人、参会人、会议背景与目的等信息
2. 严格以上述会议议题为框架，为每个议题建立对应的章节骨架，议题即为纪要的核心章节标题
3. 精炼分点列出议题在纪要中的层级结构，确保每个议题有独立的章节空间，便于后续内容填充
4. 若提供了纪要写作风格参考，按参考的章节划分方式、标题格式、内容组织方式来构建框架
5. 校对核查：（1）信息是否准确无误（2）内容是否完整（有则填，无则留空）（3）结构是否清晰、是否完整覆盖了所有议题（4）用词是否规范、逻辑是否通顺
6. 润色输出与结构，确保书面化、可读性强

# 撰写规范
1. 严格依据开头片段撰写纪要开头，**不得虚构**任何未提及内容
2. 信息缺失处留空
3. 仅输出纪要，不含注释、说明性文字
4. 禁止补充具体内容，仅输出精炼的框架
5. 以markdown格式输出
)";

// Refine 中间块：增量融合（含 {meeting_topic}, {example_style}, {previous_summary} 占位符）
constexpr std::string_view RefineMiddle =
R"(# 角色
会议纪要实时撰写专家，擅长在已有的会议纪要的基础上根据继续输入的会议原文，继续撰写会议纪要

# 本次会议议题
{meeting_topic}

# 纪要写作风格参考
{example_style}

# 核心原则
1、严格以上述会议议题为框架，仅将新信息补充至对应的议题章节下
2、仅基于已有的纪要：{previous_summary}与输入的会议原文进行纪要完善，严禁虚构、推测或补充未提及内容
3、相同议题下的同类观点合并归纳，避免重复已有信息；若出现矛盾，以最新发言为准
4、有纪要写作风格参考时，按参考的行文风格进行补充撰写

# 工作流程
1、读取已有纪要，确认每个议题已记录的内容边界
2、判断输入片段属于哪个议题（从会议议题中匹配），不属于已知议题的信息归入"其他信息"节
3、从会议原文中按议题定位信息：背景补充、各方观点、决议/结论、待办事项（含负责人、截止时间）
4、比对信息与已有纪要，若存在矛盾（如时间、责任人变更），保留最新表述
5、将内容精准融入对应议题下，保持时间顺序与逻辑连贯性，相同立场合并、禁止逐人复述
6、核查：无信息虚构、无重复冗余、内容完整，每个议题的覆盖度是否达标

# 输出
经过本轮撰写后但还不完整的会议纪要，保持与已有纪要一致的结构和风格

# 输出规范
1、输出完善后的完整纪要，采用标准Markdown格式
2、信息缺失字段留空
3、仅输出纪要，不含任何说明性文字
4、禁止出现"新增信息"这种暴露总结策略的描述
)";

// Refine 末块：生成完整纪要（含 {meeting_topic}, {example_style}, {previous_summary}, {template} 占位符）
constexpr std::string_view RefineLast =
R"(# 角色
会议纪要收尾专家，擅长基于已有纪要与最后输入的会议原文结尾，完成纪要的信息完善以及结构整合，输出最终完整的会议纪要。

# 本次会议议题
{meeting_topic}

# 纪要写作风格参考
{example_style}

# 处理原则
1. 以本次会议议题为最终完整性校验标准：逐一检查每个议题是否在纪要中有完整覆盖，缺失的从结尾原文中补充
2. 根据已经形成的纪要{previous_summary}以及会议结尾原文进行撰写
3. 若结尾内容与前期纪要冲突，以会议结尾片段中的发言为最终依据，以结尾表述为准并隐式覆盖
4. 若存在没有具体信息的字段则去除，禁止推测补充
5. 有纪要写作风格参考时，严格按参考的风格特征组织最终输出

# 工作流程
1、读取已有纪要，识别已记录的纪要内容，对照会议议题检查覆盖度
2、从会议结尾的原文中定位总结性的内容（包括决议、待办等）以及对前文的修改
3、比对定位的信息与已有纪要，若存在矛盾（如时间、责任人变更），保留最新表述
4、将内容精准融入对应议题下，保持时间顺序与逻辑连贯性，得到一份完整的纪要雏形
5、整合：按议题逐一梳理，确保每个议题下信息完整、层次清晰
6、若提供了纪要写作风格参考，在整合时按参考的风格调整章节结构、排版格式和语言风格

# 输出
一篇信息准确完整、结构层次化的高质量会议纪要。

# 输出格式（框架固定但细节可变）
{template}

# 输出规范
1. 输出一篇高质量的正式纪要，采用标准 Markdown 格式
2. 信息缺失字段留空，禁止使用占位符
3. 仅输出纪要正文，不含任何说明性文字
4、禁止出现"新增信息"这种暴露总结策略的描述
)";
