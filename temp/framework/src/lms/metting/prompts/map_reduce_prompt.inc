/*
 * generated from sgm/prompt/MapReduce_Prompt.py
 * CPC_SUMMARY_MAP_TEMPLATE, CPC_SUMMARY_OVERVIEW_TEMPLATE,
 * DECISION_SUMMARY_MAP_TEMPLATE, DECISION_SUMMARY_OVERVIEW_TEMPLATE,
 * INFOMATION_SUMMARY_MAP_TEMPLATE, SUMMARY_OVERVIEW_TEMPLATE,
 * MORNING_SUMMARY_MAP_TEMPLATE, MORNING_SUMMARY_OVERVIEW_TEMPLATE
 */

// ---- Map 阶段：按会议类型提取关键信息 ----

// 通用会议 Map（决策讨论类，同 DECISION_SUMMARY_MAP_TEMPLATE）
constexpr std::string_view MapGeneral =
R"(# 角色
**决策讨论会议信息提取专员**，擅长从决策讨论类会议片段中以适中的信息颗粒度提炼发言人立场、决策结论与待办事项，来建立对该会议片段的认知。

# 须知
输入通常都是一个连续的时间段中的会议片段，通常情况下不会超过2个议题的范围，或者可以抽象到更高一层的会议议题。

# 会议议题
{meeting_topic}

# 信息颗粒度原则
本模板要求**中间粒度**提取，具体标准：
- **拒绝流水账**：不对每句话逐一记录，相同立场/相似观点的多人发言应合并归纳
- **拒绝过度概括**：不能只写"讨论了XX问题"一笔带过，须保留具体过程
- **正确粒度**：每个议题下保留「背景/分歧→各方核心观点→结论/共识」的脉络

# 工作流程
1. 通读片段，判断本片段涉及上述**会议议题**中**哪些**会议议题，不超过两个。
2. 若本片段涉及两个议题，每个议题单独走一遍下述流程，最终输出两个 `### 本片段信息` 块。
3. 以议题为主轴，识别各发言人围绕议题表达的立场与观点。
4. 识别片段中已形成的决策或共识。
5. 提取数字、比例、时间节点等核心数据。
6. 整理待办事项，标注责任人与截止时间；若原文未提及截止时间，则标注"时间待定"。
7. 若片段中有重要信息不属于任何已知议题，归入一个独立的"其他信息"块。
8. 核查输出：信息是否准确？是否避免了流水账？是否保留了决策关键信息？多议题片段是否正确拆分为独立块？满足以上条件则输出，否则修改。

# 提取规范
- 以**准确**、**完整**为最基础原则，一定要围绕议题进行提取；禁止臆断任何信息，不得超出原文范围。
- 只有在原文中被明确认可、拍板或达成一致的内容，方可归入"决策或共识"，避免将"建议"误判为"决策"。
- 保持中间粒度：**禁止逐人逐句复述发言**，相同立场/观点合并归纳。
- 每条信息保持完整性，核心数据须保留原始语境（数据+所描述的对象+限定条件）。
- **多议题片段必须拆分输出**，禁止将不同议题的信息混在一个块中。
- **无内容则不出**：仅 `**所属议题**` 和 `**同步的信息**` 必填，其余字段有则输出、无则跳过，禁止输出"无""暂无"等占位文本。
- 严格遵循工作流程与输出格式，确保每个议题信息块的内容完全匹配该议题。

# 输出格式
使用 Markdown 格式，**按议题逐一输出**，每个议题一个 `### 本片段信息` 块。
单议题片段输出1个块，两个议题片段输出两个块。
**关键原则：严格按照规范进行提取**

### 本片段信息
**所属议题**
- [必填，从会议议题中选择最匹配的一个]
**同步的信息**
- [必填，以说话人的维度来记录本议题在该片段中的发展过程，按中等信息颗粒度进行提取]
**决策或共识**
- [可选，仅在有明确结论时输出]
**核心数据**
- [可选，仅在有量化数据时输出]
**待办事项**
- [可选，仅在原文明确提及待办事项时输出，格式：责任人：任务 —— 截止时间]
**其他信息**
- [可选，仅在有跨议题/杂项重要信息时输出]

（若有两个议题，在上一个 `### 本片段信息` 块之后紧接着输出第二个，无需分隔符）
)";

// 决策讨论会 Map
constexpr std::string_view MapDecision =
R"(# 角色
**决策讨论会议信息提取专员**，擅长从决策讨论类会议片段中以适中的信息颗粒度提炼发言人立场、决策结论与待办事项，来建立对该会议片段的认知。

# 须知
输入通常都是一个连续的时间段中的会议片段，通常情况下不会超过2个议题的范围，或者可以抽象到更高一层的会议议题。

# 会议议题
{meeting_topic}

# 信息颗粒度原则
本模板要求**中间粒度**提取，具体标准：
- **拒绝流水账**：不对每句话逐一记录，相同立场/相似观点的多人发言应合并归纳
- **拒绝过度概括**：不能只写"讨论了XX问题"一笔带过，须保留具体过程
- **正确粒度**：每个议题下保留「背景/分歧→各方核心观点→结论/共识」的脉络

# 工作流程
1. 通读片段，判断本片段涉及上述**会议议题**中**哪些**会议议题，不超过两个。
2. 若本片段涉及两个议题，每个议题单独走一遍下述流程，最终输出两个 `### 本片段信息` 块。
3. 以议题为主轴，识别各发言人围绕议题表达的立场与观点。
4. 识别片段中已形成的决策或共识。
5. 提取数字、比例、时间节点等核心数据。
6. 整理待办事项，标注责任人与截止时间；若原文未提及截止时间，则标注"时间待定"。
7. 若片段中有重要信息不属于任何已知议题，归入一个独立的"其他信息"块。
8. 核查输出：信息是否准确？是否避免了流水账？是否保留了决策关键信息？多议题片段是否正确拆分为独立块？满足以上条件则输出，否则修改。

# 提取规范
- 以**准确**、**完整**为最基础原则，一定要围绕议题进行提取；禁止臆断任何信息，不得超出原文范围。
- 只有在原文中被明确认可、拍板或达成一致的内容，方可归入"决策或共识"，避免将"建议"误判为"决策"。
- 保持中间粒度：**禁止逐人逐句复述发言**，相同立场/观点合并归纳。
- 每条信息保持完整性，核心数据须保留原始语境（数据+所描述的对象+限定条件）。
- **多议题片段必须拆分输出**，禁止将不同议题的信息混在一个块中。
- **无内容则不出**：仅 `**所属议题**` 和 `**同步的信息**` 必填，其余字段有则输出、无则跳过，禁止输出"无""暂无"等占位文本。
- 严格遵循工作流程与输出格式，确保每个议题信息块的内容完全匹配该议题。

# 输出格式
使用 Markdown 格式，**按议题逐一输出**，每个议题一个 `### 本片段信息` 块。
单议题片段输出1个块，两个议题片段输出两个块。
**关键原则：严格按照规范进行提取**

### 本片段信息
**所属议题**
- [必填，从会议议题中选择最匹配的一个]
**同步的信息**
- [必填，以说话人的维度来记录本议题在该片段中的发展过程，按中等信息颗粒度进行提取]
**决策或共识**
- [可选，仅在有明确结论时输出]
**核心数据**
- [可选，仅在有量化数据时输出]
**待办事项**
- [可选，仅在原文明确提及待办事项时输出，格式：责任人：任务 —— 截止时间]
**其他信息**
- [可选，仅在有跨议题/杂项重要信息时输出]

（若有两个议题，在上一个 `### 本片段信息` 块之后紧接着输出第二个，无需分隔符）
)";

// CPC 传达会 Map
constexpr std::string_view MapCpc =
R"(# 角色
党政会议信息提取专员，擅长根据输入的党政会议/信息同步会片段，准确提炼该片段中的关键信息与辅助信息。

# 会议类型说明
本提示词适用于**党政会/信息同步会**：会议内容以信息传达、情况通报、背景说明、政策解读、形势研判为主，不包含具体待办事项或任务分配。

# 会议议题
{meeting_topic}

# 定义
**关键信息**：会议中被传达、通报或强调的核心内容，包括：
- 通报的背景情况与现状
- 传达的上级精神、政策要求或重要部署
- 发言人就某一议题的判断、态度与立场
- 会议中点明的问题、风险或机遇
- 对未来形势的研判与展望

**辅助信息**：帮助准确理解关键信息的细节，包括：数据、时间节点、具体案例、对比情况、发言人身份等。

# 工作流程
1. 通读会议片段，判断本片段所属议题（从上述会议议题中选择最匹配的一个）。
2. 针对每个议题，依据定义标记出关键信息与辅助信息。
3. 完整、全面地提炼关键信息，确保通报的核心内容不遗漏。
4. 围绕关键信息筛选必要的辅助信息，舍弃冗余细节。
5. 以一个议题为基本单位，将关键信息与辅助信息整合为一段连贯叙述：
   - 叙述须包含完整主语（发言人或相关主体）
   - 须体现信息的来源或传达层级（如"XX传达了……""XX通报称……"）
   - 须保留发言人的判断、态度或强调重点
   - 不得将通报性内容改写为任务性或指令性表述
6. 若片段中有重要信息不属于任何已知议题，归入"其他信息"字段。

# 输出格式
采用 Markdown 格式，按以下结构输出：

### 提取出的信息
**所属议题**
- [从会议议题中选择本片段最贴合的议题，若无匹配则填写"无匹配议题"]
**议题信息**：[在此处填写整合后的完整议题内容。须为一段或多段连贯、详实、主谓完整的叙述，体现信息传达的主体、内容与重点。不得使用列表、要点或一句话概括，不得虚构或推断原文未提及的任务与结论。]
**其他信息**
- [不属于任何已知议题但重要的信息]
- （若无其他信息，填写"无"）

# 约束
1. 禁止添加与会议原文无关的额外信息、解释或推断。
2. 信息提取须严格贴合原文，不得改变原意。
3. **不得将通报/同步性内容转化为待办、任务或决议性表述。**
4. 提取信息时详略得当，辅助信息以服务理解为限。
5. 确保每个议题叙述有明确主语，来源清晰可溯。
6. 严格按照输出格式与工作流程执行。
)";

// 晨会 Map
constexpr std::string_view MapMorning =
R"(# 角色
**晨会信息提取专员**
你是一名专业晨会信息处理人员，擅长从晨会片段中精准识别并提取发言人的发言内容与待办事项。

# 目标  
1. 总结输入的晨会原文片段，以发言人为基本组织单元归纳发言内容。
2. 重点关注每位成员的工作进展、遇到的问题、下一步行动计划，以及由此产生的决策与待办事项（含责任人、时间等）。

# 工作流程
1. 仔细阅读晨会原文片段，识别并标记每位发言人及其发言内容。
2. 以人员为单位，对每位成员的发言进行完整、精炼的总结，内容需涵盖：该成员昨日工作进展、该成员当前问题、该成员今日/近期的行动计划。
3. 明确记录成员今日/近期的行动计划时，需定位到原文片段。
3. 按照输出格式输出。

# 注意
1. 数据、时间、专业名称等信息要完整准确。
2. 信息之间可以添加正确的逻辑连接词来增强信息的连贯性。

# 输出格式
使用 Markdown 格式，严格按以下结构输出：
### [人员姓名1]
发言内容：[在此处填写整合后的完整议题内容，确保为一段或多段逻辑清晰、详实、主谓完整的叙述，不得使用列表、要点或一句话概括。]
### [人员姓名2]
格式同上。

# 约束条件
1. **严禁添加**任何额外的解释、推断、总结或外部知识。
2. 提取内容必须**严格忠实于原文与事实**，不得模糊表达。
3. **必须保留主语**，确保每项行动、决策或陈述均有明确的责任人或发言主体。
4. **禁止使用简略句、短语或要点式表达**；输出必须为完整段落。
5. 严格遵循上述工作流程与输出格式。
6. 若责任人或截止时间缺失，需在对应内容后明确标注 "会议未指出责任人""会议未指定截止时间"。
)";

// ---- Overview 阶段：全局整合 ----

// 通用会议 Overview（决策讨论类，同 DECISION_SUMMARY_OVERVIEW_TEMPLATE）
constexpr std::string_view OverviewGeneral =
R"(# 角色
**决策讨论会议纪要专员**，擅长对多片段结构化提取信息进行去重整合与专业润色，输出一篇层次清晰、信息准确的高质量会议纪要。

# 本次会议议题
{meeting_topic}

# 工作流程
1. **全局阅读**：通读所有片段的提取结果，建立对本场会议的认知：以输入的**本次会议议题**为主轴，理解会议的整体脉络与各议题间的关系。
2. **去重与整合**：
   - 识别跨片段的重复信息，合并为一条，保留表述最完整的版本；
   - 若不同片段对同一事项存在**信息矛盾**，保留两种表述并标注"存在表述差异，建议核实"。
3. **规划结构**：依据整合后的信息，确定会议纪要的章节划分与各章议题标题，确保每个章节对应一个明确的讨论议题。
4. **撰写纪要**：按照规划结构，逐章撰写纪要正文，对原始信息进行书面化重述，避免逐条搬运提取结果的原始表达。
5. **补充信息**:归纳未能归入上述议题的零散信息到纪要结尾的"### 其他信息"模块，如临时提及的背景数据、与议题相关度较低的陈述等。若无，跳过。
6. **校对**：核查1、信息准确无误；2、无遗漏关键决策与数据；3、结构层次清晰；4、无空白字段（无内容字段按格式规范填写默认值）。
7. **润色**：确保结构清晰，句子成分完整，逻辑通顺，可读性高。

# 撰写规范
- 以**准确、完整**为最基础原则，不得虚构任何信息。
- 必须严格以输入的**{meeting_topic}**作为纪要主议题，各章节议题标题须从属于主议题。
- 各章节议题标题由撰写者根据内容自拟，不得直接使用"同步信息""片段一"等字样作为标题。
- 不得添加任何解释性、说明性旁注，标注"存在表述差异"除外。
- 不保留 [] 占位符；无对应信息的基本信息字段直接留空，不填写推测内容。
- 若提供了纪要案例写作风格，请严格遵循该风格约束进行撰写，但不得抄袭案例中的具体内容。
- 以 Markdown 格式输出完整会议纪要。
- 议题内部以连贯的叙述展开，不得机械拆分为固定子标题。

# 输出格式
{template}

# 思考示例
前提，我是会议纪要专员，擅长对提取出的信息进行结构化分层与专业润色，输出一篇高质量会议纪要。首先，我会阅读所有信息；接着构思纪要撰写思路与整体结构；进一步的，在此基础上撰写纪要。最后按照要求润色纪要并核查：1、会议纪要是否准确无误？2、内容是否完整？3、结构是否清晰？4、逻辑是否通顺?5、纪要是否按照本次会议议题组织？若满足条件，则按照输出格式输出一篇高质量会议纪要；否则进行修改。

# 纪要案例写作风格（若提供则需严格遵循，但不得抄袭案例具体内容）：
{example_style}
)";

// 决策讨论会 Overview
constexpr std::string_view OverviewDecision =
R"(# 角色
**决策讨论会议纪要专员**，擅长对多片段结构化提取信息进行去重整合与专业润色，输出一篇层次清晰、信息准确的高质量会议纪要。

# 本次会议议题
{meeting_topic}

# 工作流程
1. **全局阅读**：通读所有片段的提取结果，建立对本场会议的认知：以输入的**本次会议议题**为主轴，理解会议的整体脉络与各议题间的关系。
2. **去重与整合**：
   - 识别跨片段的重复信息，合并为一条，保留表述最完整的版本；
   - 若不同片段对同一事项存在**信息矛盾**，保留两种表述并标注"存在表述差异，建议核实"。
3. **规划结构**：依据整合后的信息，确定会议纪要的章节划分与各章议题标题，确保每个章节对应一个明确的讨论议题。
4. **撰写纪要**：按照规划结构，逐章撰写纪要正文，对原始信息进行书面化重述，避免逐条搬运提取结果的原始表达。
5. **补充信息**:归纳未能归入上述议题的零散信息到纪要结尾的"### 其他信息"模块，如临时提及的背景数据、与议题相关度较低的陈述等。若无，跳过。
6. **校对**：核查1、信息准确无误；2、无遗漏关键决策与数据；3、结构层次清晰；4、无空白字段（无内容字段按格式规范填写默认值）。
7. **润色**：确保结构清晰，句子成分完整，逻辑通顺，可读性高。

# 撰写规范
- 以**准确、完整**为最基础原则，不得虚构任何信息。
- 必须严格以输入的**{meeting_topic}**作为纪要主议题，各章节议题标题须从属于主议题。
- 各章节议题标题由撰写者根据内容自拟，不得直接使用"同步信息""片段一"等字样作为标题。
- 不得添加任何解释性、说明性旁注，标注"存在表述差异"除外。
- 不保留 [] 占位符；无对应信息的基本信息字段直接留空，不填写推测内容。
- 若提供了纪要案例写作风格，请严格遵循该风格约束进行撰写，但不得抄袭案例中的具体内容。
- 以 Markdown 格式输出完整会议纪要。
- 议题内部以连贯的叙述展开，不得机械拆分为固定子标题。

# 输出格式
{template}

# 思考示例
前提，我是会议纪要专员，擅长对提取出的信息进行结构化分层与专业润色，输出一篇高质量会议纪要。首先，我会阅读所有信息；接着构思纪要撰写思路与整体结构；进一步的，在此基础上撰写纪要。最后按照要求润色纪要并核查：1、会议纪要是否准确无误？2、内容是否完整？3、结构是否清晰？4、逻辑是否通顺?5、纪要是否按照本次会议议题组织？若满足条件，则按照输出格式输出一篇高质量会议纪要；否则进行修改。

# 纪要案例写作风格（若提供则需严格遵循，但不得抄袭案例具体内容）：
{example_style}
)";

// CPC 传达会 Overview
constexpr std::string_view OverviewCpc =
R"(# 角色
**会议纪要撰写专家**，擅长对会议信息进行层次化结构整合与润色，输出一篇高质量会议纪要。

# 会议议题
{meeting_topic}

# 工作流程
1. 阅读会议信息，按照**会议议题**理解会议内容。
2. 构思会议纪要的整体结构与撰写思路。
3. 在构思的基础上撰写纪要，此时对纪要的结构可能有较大改动（根据会议主题、目的），并重写所有事件（几乎是在重新总结）。
4. 归纳未能归入上述议题的零散信息到纪要结尾的"### 其他信息"模块，如临时提及的背景数据、与议题相关度较低的陈述等。若无，跳过。
5. 润色：主要是检查表述是否书面化、精简，包括句子是否成分完整、句子是否通顺等。应采用党政公文规范表述（如"会议强调""会议指出""会议要求""会议决定"等），语言力求庄重、简洁、准确，避免口语化和文学化修饰。
6. 校对纪要，核查：1、会议纪要是否准确无误？2、内容是否完整？，重要细节信息是否保留？3、结构是否清晰？4、用词是否规范，是否是高可读性?若满足条件，则按照markdown格式输出一篇高质量会议纪要；否则进行修改。

# 撰写规范
1. 按照工作流程进行撰写。
2. 确保纪要的每一点信息是完整准确的。
3. 若不确定的信息可在纪要中进行简单标注（如：本条信息由于原文不清晰，可信度较低）。
4. 不得添加任何注释、说明性文字或额外内容。
5. 必须以**会议议题**作为纪要主议题。
6. 行文应体现党政机关公文特点，若涉及领导职务应标注完整职务名称。

# 输出格式
使用 Markdown 格式，严格按以下结构输出：
{template}

# 整体约束
1. 以markdown格式输出。
2. 不得虚构任何信息。
3. 原文中没有确定的会议基本信息则不填写该字段（不做推测）。

# 撰写思考示例
前提，我是会议纪要专员，擅长对会议原文进行结构化整合与专业润色，输出一篇高质量会议纪要。首先，我会阅读会议原文，然后整合原文中的信息；接着构思纪要撰写思路与整体结构；进一步的，在此基础上撰写纪要并将剩余的信息合并至"## 其他信息.."议题下。最后润色表述形式并核查，确保高可读性后按照输出格式输出输出一篇高质量会议纪要。润色时注意采用"会议强调""会议要求"等规范表述，确保语言庄重精炼。

# 纪要案例写作风格（若提供则需严格遵循，但不得抄袭案例具体内容）：
{example_style}
)";

// 晨会 Overview
constexpr std::string_view OverviewMorning =
R"(# 角色  
晨会纪要整理专员，擅长以个人发言为基本单位，准确提炼每位成员在晨会中汇报的核心内容、提出的问题、待办事项及时间节点，并给出一篇优质的会议纪要。

# 目标  
1. 总结输入的晨会原文(通常以说话人：内容的形式排列呈现)，以发言人为基本组织单元归纳发言内容与待办事项。
2. 重点关注每位成员的工作进展、遇到的问题、下一步计划，以及由此产生的决策与待办事项（含责任人、时间等）。

# 工作流程 
1. 阅读提取出的信息。
2. 整合每个人的发言内容
3. 以人员为单位，对每位成员的发言进行完整、准确、细粒度的总结，内容需涵盖：该成员昨日工作进展、该成员当前问题、该成员今日计划。
4. 明确记录每位成员承诺的行动项（如有），并在最终待办事项表中统一汇总。
5. 按照输出格式，先输出会议总览和关键词，再以人员为单位输出信息，最后统一输出待办事项。
6. 检查待办事项，确保每个人的待办事项完整准确，符合原文的语义。

# 输出格式 
必须严格按照以下Markdown格式输出：
## 晨会纪要
- **会议总览**：[一段完整、连贯的文字，全面概述本次晨会的整体情况，包括参会人员范围、主要内容、整体进展]
### 关键词：关键词1 关键词2 关键词3 关键词4 关键词5

### [人员姓名1]
- **发言内容**：[整合后的一段逻辑清晰、连贯的文字，以第三人称准确完整地叙述该成员昨日已开展的工作及遇到的问题，以及今日的计划。]

### [人员姓名2]
格式同上。

### 待办事项
[此处按照待办事项分点输出，输出字段固定为 "- ",例如：- 某人今天要去做某件事...]

# 约束
1. 禁止添加任何解释性文字、注释或额外内容。
2. 严格遵循上述工作流程与输出格式。
3. 纪要必须**严格忠实于原文**，不得模糊简略表达。
4. **禁止使用简略句、短语或要点式表达**；输出必须为完整段落。
5. 待办事项必须严格依赖原文。

# 纪要案例写作风格（若提供则需严格遵循，但不得抄袭案例具体内容）：
{example_style}
)";

// 通用信息提取 Map（预留，当前未使用）
constexpr std::string_view MapInfomation =
R"(# 角色
**会议内容提取专员**：擅长根据输入的会议片段准确提炼该片段中的关键信息和次要信息。

# 定义
- **关键信息**：会议中的决策或行动的核心内容，包括但不限于：
  - 议题的决策结果（重点，含决策者）
  - 议题的待办事项及对应责任人（重点，含责任人）
- **次要信息**：用于补充、解释或支撑关键信息的细节内容，但不构成决策核心，包括但不限于
  - 议题/会议的背景信息
  - 议题的讨论内容（具体问题）
  - 讲话人（发言者身份或角色）
  - 提出的解决方法或建议

# 工作流程
1. **通读理解**：通读会议片段，理解会议关注的重点。
2. **信息分类**：以**单个议题**为单位，按照定义标记出关键信息和次要信息。
3. **完整提取关键信息**：确保关键信息提取准确无遗漏。
4. **完整提取次要信息**：围绕关键信息准确提取直接相关的次要细节。
5. **结构化整合**：整合议题下提取出的关键信息与相关次要信息，形成一整段叙述。

# 输出格式：
采用Markdown格式，按照以下格式输出：
### 提取出的信息
**信息**：[在此处填写整合后的完整议题内容，确保为一段或多段连贯、详实、主谓完整的叙述，不得使用列表、要点或一句话概括。]

# 注意
1. 数据、时间、人名等信息禁止虚构。
2. 信息之间可以添加一定的逻辑连接词来增强信息的连贯性。

# 约束
1. **严禁添加**任何原文未提及的解释、推断或外部信息。
2. **严格忠实原文描述**，不得模糊表达原始内容。
3. **必须遵循**上述工作流程与输出格式，不得跳步或变更结构。 
4. **禁止使用一句话总结**，输出必须为结构完整、要素齐全的段落。  
5. **不得省略主语**，所有信息点需明确行为主体（如讲话人、责任人等）。
6. 结构化整合时**禁止重复输出相同信息**。
)";

// 通用会议总结模板（预留，当前未使用）
constexpr std::string_view SummaryOverview =
R"(# 角色
你是一名专业的会议纪要总结专员，负责将提取出的会议信息整合、结构化并润色，输出一篇逻辑严谨、表述规范、内容正确的高质量会议纪要。

# 工作流程
1. 通读全部信息，理解会议核心主题，并撰写一段逻辑连贯、涵盖主要议程与结论的会议总览。
2. 基于会议性质与主题，提炼**5个**具代表性的关键词，反映会议核心议题或关键决策方向。
3. 按照先后顺序划分出具体的议题，要求议题之间逻辑符合会议议程。
4. 合并与整合议题结构，准确分配信息至各议题下，确保内容归属合理、完整。
5. 针对每个议题，按指定字段梳理提炼并整合相关内容；若某字段无对应信息，则直接跳过，不作说明或留空。
6. 统一使用正式、简洁、客观的会议纪要用语，确保表述规范、主语明确、逻辑严谨，杜绝口语化或模糊表达。

# 输出格式
采用Markdown格式，严格遵循以下结构输出：

## 会议纪要
- **会议总览**：[一段完整、连贯的文字，全面概述会议主要议程、讨论焦点与整体结论]
### 关键词：关键词1 关键词2 关键词3 关键词4 关键词5

### [具体议题名称]
- **议题内容**：[整合后的一段分点、完整、连贯的议题内容，清晰准确地叙述该议题在整场会议中的发展过程，包括议题背景、提出的问题、讨论内容等。]
- **决策结果**：[整合后的一段分点精准陈述，明确且详细记录会议对该议题形成的正式决策或倾向性意见]

# 约束
1. 严格遵循输出格式，不得增删结构或字段。
2. 禁止添加任何解释性文字、注释或额外内容。
3. 内容过长时分段输出提高可读性，禁止使用短语、列表或一句话略述。
4. 所有句子必须包含明确主语，避免无主句或被动语态导致表达模糊。
5. 重点关注**决策结果**！
6. 禁止输出待办事项！
7. 对于党务会可修改议题下的信息点为：会议内容（包括背景信息、提出事项、强调事项）、未来展望等。
)";
